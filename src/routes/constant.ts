export enum AppPages {
  RootPage = "/",
  System = "/system",
  Register = "/register",
  Login = "/login",
  Account = "/account",
}

export enum AccountPages {
  Profile = AppPages.Account + "/profile",
}
